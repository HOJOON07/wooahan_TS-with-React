## Study

```ts
declare module "*.png" {
  const src: string;
  export default src;
}
```

위 타입의 의미는 아래와 같이 이미지 애셋을 불러올 때, 기본 내보내기로 이미지의 경로를 담고 있음을 의미함.

```ts
import imagePath from "@/assets/images/some.png";
```
